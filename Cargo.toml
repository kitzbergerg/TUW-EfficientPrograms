[package]
name = "TUW-EP"
version = "0.1.0"
edition = "2021"

[dependencies]
csv = "1.3.1"
serde = { version = "1.0", features = ["derive"] }

[profile.release]
codegen-units = 1
opt-level = 3
lto = "fat"
debug = false